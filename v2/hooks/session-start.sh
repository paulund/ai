#!/bin/bash
# Injects the v2 operating behaviors into every session.
# These are loaded before any command or skill runs.

echo "[v2] Loaded operating behaviors: surface assumptions, manage confusion, push back, enforce simplicity, scope discipline, verify don't assume."
